import {
  compose,
  display,
  width,
  height,
  size,
  minWidth,
  maxWidth,
  minHeight,
  maxHeight,
  verticalAlign,
  overflow,
} from 'styled-system'

export default compose(
  display,
  width,
  height,
  size,
  minWidth,
  maxWidth,
  minHeight,
  maxHeight,
  verticalAlign,
  overflow,
)
